info:
  name: Check Reviews Metadata
  type: http
  seq: 17

http:
  method: HEAD
  url: http://localhost:4000/reviews/:comic_id
  params:
    - name: comic_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
