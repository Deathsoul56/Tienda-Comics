info:
  name: Get Reviews
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:4000/reviews/:comicId
  params:
    - name: comicId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
