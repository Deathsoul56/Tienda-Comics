info:
  name: Simular Tunnel Proxy
  type: http
  seq: 18

http:
  method: CONNECT
  url: http://localhost:4000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
