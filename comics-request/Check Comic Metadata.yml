info:
  name: Check Comic Metadata
  type: http
  seq: 16

http:
  method: HEAD
  url: http://localhost:4000/comics/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
