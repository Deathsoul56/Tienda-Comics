info:
  name: Get Health
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:4000/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
